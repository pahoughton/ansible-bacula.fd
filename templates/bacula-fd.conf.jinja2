# 2015-06-17 (cc) <paul4hough@gmail.com>
#
# This file is automatically generated by ansible-bacula.fd

# File Daemon = FD = "client" # all the same
FileDaemon {
  Name = {{ bacula_fd_name }}
  FDport = {{ bacula_fd_port }}
  WorkingDirectory = "{{ bacula_fd_working_dir }}"
  Pid Directory = "/var/run"
  Maximum Concurrent Jobs = {{ bacula_fd_max_jobs }}
}

# List Directors who are permitted to contact this File daemon
Director {
  Name = {{ bacula_dir_name }}
  Password = {{ bacula_fd_password }}
}

# Send all messages except skipped files back to Director
Messages {
  Name = Standard
  director = {{ bacula_dir_name }} = all, !skipped, !restored
}
